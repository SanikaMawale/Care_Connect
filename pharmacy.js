const express = require('express');
const router = express.Router();

// Placeholder for pharmacy routes
router.get('/', (req, res) => res.json({ msg: 'Pharmacy routes' }));

module.exports = router;